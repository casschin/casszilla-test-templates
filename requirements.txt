# This pulls in all the libraries needed to run Selenium tests
# on Mozilla WebQA projects

BeautifulSoup4==4.2.1    # Only required for doing link checking without Selenium
py==1.4.9
pytest==2.2.4
pytest-mozwebqa==1.1.1
PyYAML==3.10
requests==2.20.0
selenium
UnittestZero
